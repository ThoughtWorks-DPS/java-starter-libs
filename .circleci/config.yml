version: 2.0
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk-browsers
    steps:
      - checkout
      - run:
          command: ./gradlew clean build
      - run:
          command: ./gradlew publish
